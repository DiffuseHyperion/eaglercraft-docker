FROM ubuntu:18.04

ENV MIN_RAM=512M
ENV MAX_RAM=512M
ENV EULA=FALSE
ENV ONLINE=true

VOLUME /server
EXPOSE 25565

COPY update.sh /update.sh
COPY . /server

CMD /update.sh $MIN_RAM $MAX_RAM $EULA $ONLINE

